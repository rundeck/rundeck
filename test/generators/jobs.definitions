<command><exec>if ! ssh ${option.standbyServer}  uname -a; then echo "cannot connect to the standby server, ${option.standbyServer}, will not sync"; exit 1; fi</exec></command>
<command><exec>whoami</exec></command>
<command><exec>uname -a</exec></command>