rundeck:
  grails:
    upload:
      maxsize: '50000000'
  server:
    name: superserver
  config:
    storage:
      converter:
        '1':
          config:
            password: 'blahblah'
  storage:
    provider:
      '2':
        type: 'file'
  features:
    repository:
      enabled: "true"