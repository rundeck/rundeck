package rundeck.data.util

class OptionsUtilTests {

    // Tests are found in the FrameworkServiceTests class
    // in the main project because the code originally came
    // from there and the tests are written in junit
}
