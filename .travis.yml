language: java
env: MAVEN_OPTS="-Xmx512m -XX:MaxPermSize=192m" GRADLE_OPTS="-Xmx512m -XX:MaxPermSize=192m"
jdk:
  - openjdk7
  - openjdk6

# whitelist
branches:
  only:
    - development

notifications:
  irc:
    - "irc.freenode.org#rundeck"
  #email: false


