buildscript {
    repositories {
        mavenLocal()
        mavenCentral()
        maven { url "https://repo.grails.org/grails/core" }
        maven { url "https://repo.grails.org/grails/restricted" }
    }
    dependencies {
        classpath platform("org.apache.grails:grails-bom:$grailsVersion")
        classpath "org.apache.grails:grails-gradle-plugins:$grailsVersion"
        classpath "org.apache.grails:grails-events:$grailsVersion"
    }
}

plugins {
    id "org.springframework.boot" version "${springBootVersion}"
}

apply plugin:"eclipse"
apply plugin:"idea"
apply plugin:"org.apache.grails.gradle.grails-plugin"
apply plugin:"org.apache.grails.gradle.grails-gsp"

apply from:'../gradle/grailsopenapispec.gradle'

repositories {
    mavenLocal()
    mavenCentral()
    maven { url "https://repo.grails.org/grails/core" }
    maven { url "https://repo.grails.org/grails/restricted" }
}

configurations{
    //define libs needed to be bundled into the WEB-INF/lib dir of the war
    exportedLibs
    compile{
        extendsFrom exportedLibs
    }
    developmentOnly
    runtimeClasspath {
        extendsFrom developmentOnly
    }
    all {
        exclude group: 'org.springframework.boot', module: 'spring-boot-starter-logging'
        exclude group: 'ch.qos.logback', module: 'logback-classic'
        exclude group: 'ch.qos.logback', module: 'logback-core'
    }
    provided
}

apply from: "../gradle/java-version.gradle"

dependencies {

    implementation platform("org.apache.grails:grails-bom:${grailsVersion}")
    implementation project(":core")

    implementation 'org.quartz-scheduler:quartz:2.3.2'
    implementation 'org.grails.plugins:quartz:2.0.13'

    implementation "org.springframework.boot:spring-boot-starter-log4j2"
    implementation "org.springframework.boot:spring-boot-autoconfigure"
    implementation "jakarta.annotation:jakarta.annotation-api:${jakartaAnnotationVersion}"
    implementation "io.swagger.core.v3:swagger-annotations:${swaggerVersion}"
    implementation "org.apache.grails:grails-core"
    implementation "org.apache.grails:grails-events"
    annotationProcessor "org.apache.grails.events:grails-events-transforms"
    implementation "org.springframework.boot:spring-boot-starter-actuator"
    implementation "org.apache.grails:grails-web-boot"
    implementation "org.apache.grails:grails-logging"
    implementation "org.apache.grails:grails-rest-transforms"
    implementation "org.apache.grails:grails-databinding"
    implementation "org.apache.grails:grails-core"
    implementation "org.apache.grails:grails-services"
    implementation "org.apache.grails:grails-url-mappings"
    implementation "org.apache.grails:grails-interceptors"
    implementation "org.apache.grails:grails-cache"
    implementation "org.apache.grails:grails-async"
    implementation "org.apache.grails:grails-scaffolding"
    implementation "org.apache.grails:grails-gsp"
    console "org.apache.grails:grails-console"
    implementation "org.yaml:snakeyaml:${snakeyamlVersion}"
    compileOnly "io.micronaut:micronaut-inject-groovy:${micronautVersion}"
    implementation "org.apache.groovy:groovy-dateutil:${project.property('groovy.version')}"

    profile "org.grails.profiles:web-plugin"
    provided "org.apache.grails:grails-services"
    provided "org.apache.grails:grails-domain-class"


    testImplementation "org.apache.grails:grails-testing-support-datamapping"
    testImplementation "org.mockito:mockito-core"
    testImplementation "org.apache.grails:grails-testing-support-web"
}

bootRun {
    ignoreExitValue true
    jvmArgs(
            '-Dspring.output.ansi.enabled=always',
            '-noverify',
            '-XX:TieredStopAtLevel=1',
            '-Xmx1024m')
    sourceResources sourceSets.main
    String springProfilesActive = 'spring.profiles.active'
    systemProperty springProfilesActive, System.getProperty(springProfilesActive)
}
// enable if you wish to package this plugin as a standalone application
bootJar.enabled = false

tasks.withType(Test) {
    useJUnitPlatform()
}

tasks.withType(GroovyCompile).configureEach {
    options.incremental = false
}

// Fix Gradle 8 task ordering for OpenAPI generation
// The OpenAPI file is generated during compileGroovy (main), but compileTestGroovy
// was running in parallel. Make sure test compilation happens AFTER main tasks.
tasks.named("findMainClass") {
    mustRunAfter tasks.named("compileTestGroovy")
}
tasks.named("jar") {
    mustRunAfter tasks.named("compileTestGroovy")
}
tasks.named("compileGroovyPages") {
    mustRunAfter tasks.named("compileTestGroovy")
}

jar {
    enabled = true
    archiveClassifier = '' //use empty string
}
